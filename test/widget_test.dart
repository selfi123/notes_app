import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Voicecard requires Hive and dotenv initialization before pumping,
    // so a full widget test requires test setup. This is a placeholder.
    expect(true, isTrue);
  });
}
